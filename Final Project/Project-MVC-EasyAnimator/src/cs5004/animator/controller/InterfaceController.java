package cs5004.animator.controller;

/**
 * This class represents the interface for a controller for an animation.
 */
public interface InterfaceController {
  /**
   * Runs the current animation based on how the controller sees fit.
   */
  void start();
}
